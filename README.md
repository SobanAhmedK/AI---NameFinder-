## NameFinder

This repo helps you out to find the best name using AI and also check if the domain is available on the GoDaddy.


## Installation
1. Make conda env or any other virtual env.
   1. To create virtual env in Conda follow these commands
   2. Install Conda
   3. `conda create -n nameFinder python=3.10.6`
2. Use python=3.10.6
3. `pip install -r requirements.txt`    
   

## Contribution Requiremets
1. Install precommits `pre-commit install`
2. Check if all testcases pass